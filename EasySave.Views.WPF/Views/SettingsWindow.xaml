<Window x:Class="EasySave.Views.WPF.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Log format -->
        <Label Grid.Row="0" Content="Log format:" FontWeight="SemiBold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,12">
            <RadioButton x:Name="RbJson" Content="JSON" IsChecked="True" Margin="0,0,16,0"/>
            <RadioButton x:Name="RbXml"  Content="XML"/>
        </StackPanel>

        <!-- Save button -->
        <Button Grid.Row="3" Content="Save" Width="80"
                HorizontalAlignment="Right" Click="BtnSave_Click"/>
    </Grid>
</Window>
